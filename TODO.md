# TODO

- [x] Inspect existing `.github/workflows/ci.yml`
- [x] Fix malformed workflow YAML (it previously contained docker-compose content)
- [x] Add frontend job: setup node, npm ci, npm run lint, npm run build
- [x] Add backend job: setup python, install requirements-dev, run migrations, run Django pytest, install FastAPI requirements-dev, run FastAPI pytest

